"""Vault extension entities.

Most vault data (tokens, balances, transfers) is now managed by
basilisk OS Wallet entities (Token, WalletBalance, WalletTransfer).
This module is kept minimal for any vault-specific admin state.
"""
